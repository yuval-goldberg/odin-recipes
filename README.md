This is my first finished project on the odin project
Unfortunately, I forgot to commit snapshots to the projects, so the first push to the repo is the finished one.

I used a lot of HTML elements and practiced a little of the git commands on the terminal.